#!/usr/bin/env ruby
# frozen_string_literal: true

# Inspects the github event.

if ENV.key?("DEBUG_COMMIT_TOOLS")
  require "pathname"

  event_path = Pathname.new(ENV["GITHUB_EVENT_PATH"])

  if event_path.exist?
    puts event_path.read
  end
end
